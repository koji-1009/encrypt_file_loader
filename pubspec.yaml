name: encrypt_file_loader
description: encrypt_file_loader is a library that can store files encrypted with webcrypto.
version: 2.2.1
homepage: https://github.com/koji-1009/encrypt_file_loader

environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=2.10.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.0

  # db
  drift: ^1.5.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.0.0
  path: ^1.8.0

  # decrypt
  webcrypto:
    git:
      url: https://github.com/koji-1009/webcrypto.dart
      ref: webcrypto_js_type
  freezed_annotation: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  drift_dev: ^1.5.0
  freezed: ^2.0.0
  build_runner: ^2.0.0

  flutter_lints: ^1.0.0

flutter:
